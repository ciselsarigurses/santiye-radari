name: Seed Morfoloji Ham Skor Headroom Diagnostiği

on:
  push:
    branches: [main]
    paths:
      - "seed_centered_excavation_morphology_review.json"
      - "seed_morphology_headroom_audit.py"
      - ".github/workflows/seed-morphology-headroom.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: santiye-radari-seed-morphology-headroom
  cancel-in-progress: false

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Depoyu al
        uses: actions/checkout@v6
        with:
          ref: main

      - name: Python kur
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Ham skor diagnostiği öz testi
        run: python seed_morphology_headroom_audit.py --check-only

      - name: Morfoloji tavan headroomunu ölç
        run: python seed_morphology_headroom_audit.py

      - name: Diagnostiği güvenli kaydet
        run: |
          output="seed_morphology_headroom_review.json"
          if [[ -z "$(git status --porcelain -- $output)" ]]; then
            echo "Headroom diagnostiği değişmedi."
            exit 0
          fi

          git config user.name "santiye-radari-bot"
          git config user.email "santiye-radari-bot@users.noreply.github.com"

          for attempt in 1 2 3; do
            git fetch origin main
            git reset --hard origin/main
            python seed_morphology_headroom_audit.py --check-only
            python seed_morphology_headroom_audit.py

            if [[ -z "$(git status --porcelain -- $output)" ]]; then
              echo "Güncel main üzerinde kayıt gerektiren fark kalmadı."
              exit 0
            fi

            git add $output
            git commit -m "Radar: morfoloji ham skor headroom diagnostiğini güncelle"
            if git push origin HEAD:main; then
              echo "Headroom diagnostiği kaydedildi."
              exit 0
            fi

            echo "Ana dal ilerledi; yeniden denenecek."
            sleep $((attempt * 2))
          done

          echo "Üç denemede diagnostik kaydı tamamlanamadı."
          exit 1
